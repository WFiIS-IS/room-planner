import { PageWrapper } from '@/components/PageWrapper';
import { mainLogger } from '@/lib/logger';

const logger = mainLogger.child({ name: "'/': <Home />" });

export default function Home() {
  return <PageWrapper>TODO</PageWrapper>;
}
